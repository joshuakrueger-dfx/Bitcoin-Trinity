[package]
name         = "trinity-verify"
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true

[dependencies]
# BEWUSST OHNE miniscript — der Verifier leitet unabhängig ab (SPECIFICATION.md §1.5).
# Erzwungen durch deny.toml [bans]. Ein Verstoß bricht den Build, nicht das Review.
trinity-types = { path = "../trinity-types" }
bitcoin = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
